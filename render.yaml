services:
  - type: web
    name: ai-weather-outfit
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 120
    envVars:
      # 🔧 修正: Gemini APIを使用するため、GOOGLE_API_KEYに変更
      - key: GOOGLE_API_KEY
        sync: false
      - key: PORT
        value: 10000
