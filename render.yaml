services:
  - type: web
    name: ai-weather-outfit
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    # タイムアウトを120秒に延長して、AIの生成待ちによるクラッシュを防ぐ
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: OPENAI_API_KEY
        sync: false
